#SIMPLE COUNTER

Create a counter application in TypeScript using hooks. The counter has three buttons: -, +, and X which decrement (limit: -100), increment (limit: +100) and reset the counter value. It displays the current counter value too. The initial value of the counter is 0.
